# dependencies
node_modules/

# build output
dist/
.astro/

# generated content (regen via scripts)
# (keep content/* committed so the site is reproducible)

# env / secrets
.env
.env.*
!.env.example

# os
.DS_Store
Thumbs.db

# editor
.vscode/
.idea/

# logs
npm-debug.log*
*.log
.secrets.local.md
# Note: data/.indexnow-key IS committed deliberately — the IndexNow key is
# already public (it's hosted at /<key>.txt). The protocol is a shared
# publish, not a secret.
